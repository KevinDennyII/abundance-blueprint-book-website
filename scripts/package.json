{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "cleanup:frontend": "tsx ./src/cleanup.ts",
    "cleanup:frontend:apply": "tsx ./src/cleanup.ts --apply-safe-fixes",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
