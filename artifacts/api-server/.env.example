PORT=5050
KIT_API_KEY=
KIT_CHAPTER1_FORM_ID=9557935
KIT_CIRCLE_FORM_ID=9564646

# Blog / admin (Postgres + session)
# Local Docker: docker compose up -d  →  postgres on localhost:5434
DATABASE_URL=postgres://abundance:abundance@localhost:5434/abundance_blog
SESSION_SECRET=change-me-to-a-long-random-string
ADMIN_EMAIL=admin@local.test
ADMIN_PASSWORD=localadmin123

# Passkeys (WebAuthn). Production needs HTTPS and your real domain.
# WEBAUTHN_ORIGIN can be a comma-separated list.
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Abundance Blueprint Admin
WEBAUTHN_ORIGIN=http://localhost:5173,http://localhost:5174
